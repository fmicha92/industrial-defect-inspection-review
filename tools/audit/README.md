# Dataset audit helpers

`audit_dataset_release.py` inspects local image releases and common annotation formats. It requires NumPy and Pillow, which are intentionally not dependencies of the core repository package.

Run it only on lawfully obtained data stored outside version control, preferably under `workspace/`.

