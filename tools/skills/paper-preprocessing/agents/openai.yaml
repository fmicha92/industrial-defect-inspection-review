interface:
  display_name: "Paper Preprocessing"
  short_description: "Ingest and preprocess papers reproducibly"
  default_prompt: "Use $paper-preprocessing to scan and prepare the local paper intake queue."
policy:
  allow_implicit_invocation: true

