# Schemas

The JSON Schemas describe the interoperable top-level properties used by dataset and paper notes. They are documentation and validation targets for future structured ingestion; Markdown remains the canonical source format.

The current exporter intentionally accepts incomplete historical notes and reports coverage gaps instead of rejecting the entire vault. New or substantially revised notes should satisfy the relevant schema wherever the source evidence permits.

